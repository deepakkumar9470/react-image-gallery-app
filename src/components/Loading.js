import React from 'react'

const Loading = () => {
    return (
        <div style={{fontSize : '30px',textAlign : 'center'}}>
               <p>Loading awesome images please wait...</p>
        </div>
    )
}

export default Loading
